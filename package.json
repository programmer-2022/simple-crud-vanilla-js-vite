{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db.json --port 5001"
  },
  "devDependencies": {
    "json-server": "^0.17.3",
    "vite": "^4.2.0"
  },
  "dependencies": {
    "sweetalert2": "^11.7.3"
  }
}
